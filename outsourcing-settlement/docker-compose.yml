services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: outsourcing
      POSTGRES_USER: outsourcing
      POSTGRES_PASSWORD: outsourcing
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U outsourcing"]
      interval: 3s
      timeout: 3s
      retries: 20

  backend:
    build: ./backend
    environment:
      POSTGRES_HOST: db
      POSTGRES_DB: outsourcing
      POSTGRES_USER: outsourcing
      POSTGRES_PASSWORD: outsourcing
      DJANGO_DEBUG: "0"
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:80"

volumes:
  pgdata:
